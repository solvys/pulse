# ============================================
# Pulse Backend - Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git

# ============================================
# Required Variables
# ============================================

# Neon PostgreSQL Database (Required)
# Get from: https://console.neon.tech → Connection Details
# Format: postgres://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
NEON_DATABASE_URL=postgres://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# Clerk Authentication Secret Key (Required)
# Get from: https://dashboard.clerk.com → API Keys → Secret Keys
# Used for verifying JWT tokens from frontend
CLERK_SECRET_KEY=sk_live_YOUR_CLERK_SECRET_KEY

# ============================================
# Optional Variables (with defaults)
# ============================================

# CORS Origins (Optional, default: http://localhost:3000)
# Comma-separated list of allowed frontend origins
# For production, include your Vercel deployment URL
CORS_ORIGINS=http://localhost:3000,https://pulse.solvys.io

# Server Port (Optional, default: 8080)
# Fly.io sets this automatically in production
PORT=8080

# Node Environment (Optional, default: development)
# Set to 'production' in Fly.io deployment
NODE_ENV=development

# Log Level (Optional, default: info)
# Options: debug, info, warn, error
LOG_LEVEL=info

# ============================================
# ProjectX Trading API (Optional)
# ============================================
# Get from your ProjectX/TopStepX account
# Only needed if using ProjectX trading features
PROJECTX_USERNAME=your_projectx_username
PROJECTX_API_KEY=your_projectx_api_key

# ============================================
# Clerk Publishable Key (Optional)
# ============================================
# Not typically needed in backend, but available if needed
# Frontend uses this, backend uses CLERK_SECRET_KEY
CLERK_PUBLISHABLE_KEY=pk_live_YOUR_CLERK_PUBLISHABLE_KEY

# ============================================
# Fly.io Deployment Notes
# ============================================
# For Fly.io deployment, set secrets using:
#   fly secrets set NEON_DATABASE_URL="..." -a pulse-api-withered-dust-1394
#   fly secrets set CLERK_SECRET_KEY="..." -a pulse-api-withered-dust-1394
#   fly secrets set CORS_ORIGINS="..." -a pulse-api-withered-dust-1394
#
# View current secrets:
#   fly secrets list -a pulse-api-withered-dust-1394
#
# Required Fly.io secrets:
#   - NEON_DATABASE_URL (required)
#   - CLERK_SECRET_KEY (required)
#   - CORS_ORIGINS (recommended for production)
#
# Optional Fly.io secrets:
#   - PROJECTX_USERNAME
#   - PROJECTX_API_KEY
#   - LOG_LEVEL
