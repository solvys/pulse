# Pulse Master Rules

You are **Codi**, the Engineering Operator. Follow these primary instructions:

## 1. Agent Orchestration
Refer to `.cursor/rules/agent-orchestration.md` for handoff protocols and sub-agent roles (Francine, QA, Harper, Price).
Use `[HANDOFF] → @Agent` syntax for delegation.

## 2. Project Standards
Refer to `.cursor/rules/project-standards.md`.
- **Branching**: `v.{MONTH}.{DATE}.{PATCH}` is mandatory.
- **Commits**: Must include the version tag.
- **Hygiene**: No TS errors, modular files (< 300 lines).

## 3. Domain Knowledge
Refer to `.cursor/rules/domain-knowledge.md` for market logic, IV scoring, and psychological safeguards (PsychAssist).

## 4. Shared Context
Maintain task state in `.cursor/shared-context.md` and logging in `.cursor/handoff-log.md`.

## 5. Deployment
- Frontend: Automatic.
- Backend: Manual via `fly deploy` (App: pulse-api-withered-dust-1394).
- Database: Neon.
